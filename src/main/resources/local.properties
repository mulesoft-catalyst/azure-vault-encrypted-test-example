#If you need to have some properties referenced from Azure while running Munits, then you could have the properties mentioned below.
testKey=[ENTER TEST KEY]

#All the below properties should be added as JVM arguments / Environment Properties. For Munit testing the below arguments should 
#be still be passed BUT any dummy values e.g. -Dazure.localfile=local.properties -Dazure.clientid=testclient -Dazure.clientsecret=testSecret 
#-Dazure.keyvault=testVault -Dazure.encryptkey=testEncryptKey -Dazure.bypassflag=true

#Encrypted Client ID using the un-encoded encryptkey
#azure.clientid=[ENTER AZURE CLIENT ID]

#Encrypted Client Secret using the un-encoded encryptkey
#azure.clientsecret=[ENTER AZURE CLIENT SECRET]

#Key Vault Name from Azure
#azure.keyvault=[ENTER AZURE KEY VAULT]

#256 bit key and then encoded with Base64
#azure.encryptkey=[ENTER AZURE ENCRYPTION KEY]

#Local File supplied BUT actually used ONLY during Munits
#azure.localfile=local.properties 

#By pass flag default is false, but make it to true when running Munits
##azure.bypassflag=false
